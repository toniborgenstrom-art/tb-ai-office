"use client";

import { useRouter } from "next/navigation";
import { useState } from "react";

export type RegisteredOffer = { id: string; title: string; offerNumber: string; amount: number | null; expiresAt: string; status: string; source: string; projectName: string };
const labels: Record<string, string> = { new: "Uusi tarjouspyyntö", draft: "Luonnos", sent: "Lähetetty", won: "Voitettu", lost: "Hävitty", declined: "Ei jatkoon" };

export function OfferRegister({ offers }: { offers: RegisteredOffer[] }) {
  const router = useRouter(); const [editing, setEditing] = useState<RegisteredOffer | null>(null); const [saving, setSaving] = useState(false); const [error, setError] = useState("");
  async function save(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (!editing) return; setSaving(true); setError(""); const form = new FormData(event.currentTarget); const response = await fetch(`/api/offers/${editing.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status: form.get("status"), offerNumber: form.get("offerNumber"), amount: form.get("amount"), expiresAt: form.get("expiresAt") }) }); const data = await response.json(); if (!response.ok) setError(data.error || "Tarjouksen päivitys epäonnistui."); else { setEditing(null); router.refresh(); } setSaving(false); }
  const euro = (amount: number | null) => amount === null ? "—" : amount.toLocaleString("fi-FI", { style: "currency", currency: "EUR" });
  const active = offers.filter((offer) => ["draft", "sent"].includes(offer.status)); const sentValue = offers.filter((offer) => offer.status === "sent").reduce((total, offer) => total + (offer.amount ?? 0), 0);
  return <><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-[#a67a00]">Tarjousten hallinta</p><h1 className="mt-2 text-3xl font-semibold">Tarjoukset</h1><p className="mt-2 max-w-3xl text-sm text-[#607188]">Seuraa tarjousluonnoksia ja lähetettyjä tarjouksia. Päivitä tila, tarjousnumero, arvo ja voimassaolo yhdestä paikasta.</p></div><section className="mt-8 grid gap-4 sm:grid-cols-3"><div className="border border-[#d6dee7] border-t-4 border-t-[#d9ae16] bg-white p-5"><p className="text-xs font-semibold uppercase text-[#607188]">Aktiiviset tarjoukset</p><p className="mt-2 text-3xl font-semibold">{active.length}</p><p className="mt-1 text-xs text-[#607188]">Luonnokset ja lähetetyt</p></div><div className="border border-[#d6dee7] border-t-4 border-t-[#70a7e8] bg-white p-5"><p className="text-xs font-semibold uppercase text-[#607188]">Lähetetty tarjouskanta</p><p className="mt-2 text-3xl font-semibold">{euro(sentValue)}</p><p className="mt-1 text-xs text-[#607188]">Lähetettyjen tarjousten arvo</p></div><div className="border border-[#d6dee7] border-t-4 border-t-[#1f6b56] bg-white p-5"><p className="text-xs font-semibold uppercase text-[#607188]">Voitetut</p><p className="mt-2 text-3xl font-semibold">{offers.filter((offer) => offer.status === "won").length}</p><p className="mt-1 text-xs text-[#607188]">Seurannassa</p></div></section>{error && <p className="mt-5 border border-red-300 bg-red-50 p-3 text-sm text-red-800">{error}</p>}<div className="mt-8 overflow-x-auto border border-[#d6dee7] bg-white"><div className="min-w-[860px] grid grid-cols-[1.4fr_.8fr_.65fr_.7fr_.7fr_auto] gap-4 border-b bg-[#f4f7fa] px-5 py-3 text-xs font-semibold uppercase text-[#607188]"><span>Tarjous</span><span>Kohde / lähde</span><span>Voimassa</span><span>Summa</span><span>Tila</span><span></span></div>{offers.length ? offers.map((offer) => <div key={offer.id} className="min-w-[860px] grid grid-cols-[1.4fr_.8fr_.65fr_.7fr_.7fr_auto] items-center gap-4 border-b border-[#e4e9ef] px-5 py-4 text-sm"><div><b>{offer.offerNumber || offer.title}</b>{offer.offerNumber && <p className="mt-1 text-xs text-[#607188]">{offer.title}</p>}</div><span className="text-[#607188]">{offer.projectName || offer.source || "—"}</span><span>{offer.expiresAt || "—"}</span><span>{euro(offer.amount)}</span><span className="rounded bg-[#eef3f7] px-2 py-1 text-xs font-medium text-[#253d58]">{labels[offer.status] || offer.status}</span><button onClick={() => setEditing(offer)} className="text-xs font-semibold text-[#142b45] underline">Päivitä</button></div>) : <p className="p-8 text-sm text-[#607188]">Ei vielä tarjouksia. Tarjousvahdin löydös voidaan siirtää seuraavaksi tarjousluonnokseksi.</p>}</div>{editing && <div className="fixed inset-0 z-50 grid place-items-center bg-[#142b45]/55 p-4"><form onSubmit={save} className="w-full max-w-md bg-white p-6 shadow-2xl"><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#a67a00]">Tarjouskanta</p><h2 className="mt-1 text-xl font-semibold">Päivitä tarjous</h2><p className="mt-2 text-sm text-[#607188]">{editing.title}</p><label className="mt-5 block text-sm font-medium">Tarjousnumero<input name="offerNumber" defaultValue={editing.offerNumber} placeholder="TB-2026-001" className="mt-2 w-full border border-[#cfd8e2] p-3" /></label><label className="mt-3 block text-sm font-medium">Summa, alv 0 %<input name="amount" type="number" min="0" step="0.01" defaultValue={editing.amount ?? ""} className="mt-2 w-full border border-[#cfd8e2] p-3" /></label><label className="mt-3 block text-sm font-medium">Voimassa asti<input name="expiresAt" type="date" defaultValue={editing.expiresAt} className="mt-2 w-full border border-[#cfd8e2] p-3" /></label><label className="mt-3 block text-sm font-medium">Tila<select name="status" defaultValue={editing.status} className="mt-2 w-full border border-[#cfd8e2] p-3">{Object.entries(labels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><div className="mt-6 flex gap-3"><button disabled={saving} className="rounded bg-[#d9ae16] px-4 py-3 font-semibold">{saving ? "Tallennetaan…" : "Tallenna muutokset"}</button><button type="button" onClick={() => setEditing(null)} className="text-sm underline">Peruuta</button></div></form></div>}</>;
}
